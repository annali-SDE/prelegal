services:
  prelegal:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - FRONTEND_DIST_DIR=./frontend_dist
      - DB_PATH=./prelegal.db
    restart: unless-stopped
